import { Button } from "@douyinfe/semi-ui";
import { NavigationBar } from "@/component/base";

export const Chat = () => {
  return (
    <div>
      <NavigationBar />
      <Button>chat</Button>
    </div>
  );
};
